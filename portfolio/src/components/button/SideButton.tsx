"use client";

// Backwards-compatible wrapper so old imports using
// "@/components/button/SideButton" keep working.
// Internally we just reuse the newer SideButtonTest implementation.

export { default } from "./SideButtonTest";

